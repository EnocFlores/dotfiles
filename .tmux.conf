# EnocFlores <https://github.com/EnocFlores>
# Last Change: 2023.12.06

# === Set bind key to C-t ====================== #
unbind C-b
set -g prefix C-t
bind C-t send-prefix

# === Set terminal 256 color compatible      === #
set -g default-terminal "screen-256color"

# === Reload config ============================ #
bind r source-file ~/.tmux.conf

# === Set local time =========================== #
set -g status-right ' "#H" 󰅐 [#(TZ="America/New_York" date +"%%Y-%%m-%%d %%H:%%M")]'

# === Split panes using | and - ================ #
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# === Act like vim ============================= #
setw -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection
# bind-key -T vi-copy 'v' begin-selection     # Begin selection in copy mode.
# bind-key -T vi-copy 'C-v' rectangle-toggle  # Begin selection in copy mode.
# bind-key -T vi-copy 'y' copy-selection      # Yank selection in copy mode.

# === Move between panes ======================= #
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# === Resize panes ============================= #
bind -r C-h resize-pane -L 5
bind -r C-j resize-pane -D 5
bind -r C-k resize-pane -U 5
bind -r C-l resize-pane -R 5

# === Maximize selected pane =================== #
bind -r m resize-pane -Z

# === Allow the use of mouse =================== #
set -g mouse on

# === Plugins ================================== #
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @resurrect-capture-pane-contents 'on' # allow tmux-ressurect to capture pane contents
set -g @continuum-restore 'on' # enable tmux-continuum functionality

# ============================================== #
# === Initialize TMUX plugin manager (keep  === #
# === this line at the very bottom of       === #
# === tmux.conf)                            === #
# ============================================== #
run '~/.tmux/plugins/tpm/tpm'
